@using System.Data.SqlClient;
@if(IsPost){
    try{
    var StuID = Request.Form.Get("StuID");
    var Score = Request.Form.Get("Score");
    var SubID = Request.Form.Get("SubID");
    var ID = Request.Form.Get("ID");
    
    var db = WebMatrix.Data.Database.Open("Student_DB");
    var stus = db.Query("UPDATE Results SET StudentID=@0, SubjectID=@1, Score=@2 WHERE Id=@3",StuID, SubID, Score, ID);
        Response.Redirect("~/Scores/default.cshtml");
    }catch(Exception ex){
        Response.Write(ex.Message);
    }

}